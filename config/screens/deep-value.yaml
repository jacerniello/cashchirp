# Deep value
#
# Saved from the screener UI. This is the SAME format the idea board reads, so it
# runs unchanged at /screener/ideas/deep-value.
# Edit it by hand freely - see docs/CONFIGURATION.md for every option.
id: deep-value
title: Deep value
description: Cheap on earnings and cash flow, ignoring quality.
universe:
  exclude_delisted: true
gates:
  pe:
    min: 0.0
    max: 12.0
  p_fcf:
    min: 0.0
    max: 10.0
